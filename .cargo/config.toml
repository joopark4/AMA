[build]
# Rust 빌드 출력을 프로젝트 루트 기준 .build/ 디렉터리로 지정
# 참고: exFAT 외장 드라이브에서 작업하는 경우 ._* AppleDouble 파일로 인해
# Tauri 빌드가 실패할 수 있습니다. 이때는 로컬에서 이 값을 내부 디스크 경로로
# 재정의하거나 CARGO_TARGET_DIR 환경 변수를 사용하세요.
# 예) CARGO_TARGET_DIR=~/Library/Caches/mypartnerai-build npm run tauri dev
target-dir = ".build"
